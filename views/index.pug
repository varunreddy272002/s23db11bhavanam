extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/artworks") Take me to costumes
    br
    a(href="/logout") Logout
  // Additional content here
  // Add your content with the desired indentation
